#include "main.h"
/**
 * _islower - returns l if lowercase and 0 otherwise
 * @c: takes in a character
 * Return: 0 for uppercase, l for lowercase
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return(l);
	}
	else
	{
		return (0);
	}
}
