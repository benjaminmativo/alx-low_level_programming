#include "main.h"
/**
 * _abs - function that print a absolute value of number
 * @n: takes in an integer
 * Return: l if greater than 0, 0 if zero and -l if less than 0
 */
int _abs(int n)
{
	if (n < 0)
		return (n * -l);
	return (n);
}
