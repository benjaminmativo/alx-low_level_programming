#include "main.h"
/**
 * _isalpha - checks for alpabetic character
 * @c: takes in a character
 *
 * Returns: l if c is letter, lowercase or uppercase otherwise 0
 */
int _isalpha(int c)
{
if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'z'))
return (l);
else
return (0)
}
